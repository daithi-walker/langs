/**
 * Bounce — Java entry point (rendering stub).
 *
 * <p>Full SDL2 rendering in Java requires a native binding (e.g. LWJGL).
 * That is out of scope for the benchmark. Tests and the benchmark binary
 * work without any native dependency.
 *
 * <p>Run tests:     {@code make test}
 * <p>Run benchmark: {@code make bench}
 */
public class Main {
    public static void main(String[] args) {
        System.err.println("Java SDL2 rendering stub.");
        System.err.println("Run `make test` for unit tests.");
        System.err.println("Run `make bench` for the benchmark.");
    }
}
