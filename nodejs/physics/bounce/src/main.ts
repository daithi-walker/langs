/**
 * @file   main.ts
 * @brief  Bounce — Node.js entry point (rendering stub).
 *
 * Full SDL2 rendering in Node requires a native binding (e.g. node-sdl2).
 * Tests and the benchmark work standalone via ts-node / tsc.
 *
 * Run tests:     npx vitest run
 * Run benchmark: npx ts-node src/bench.ts
 */

console.error("Node.js SDL2 rendering stub.");
console.error("Run `npx vitest run` for unit tests.");
console.error("Run `npx ts-node src/bench.ts` for the benchmark.");
