"""Bounce — Python entry point (rendering stub).

Full SDL2 rendering in Python is available via pygame or pysdl2, but is
out of scope for the benchmark. Tests and the benchmark work standalone.

Run tests:     pytest tests/
Run benchmark: python bench.py
"""

if __name__ == "__main__":
    import sys
    print("Python SDL2 rendering stub.", file=sys.stderr)
    print("Run `pytest tests/` for unit tests.", file=sys.stderr)
    print("Run `python bench.py` for the benchmark.", file=sys.stderr)
